# DSH 桌面版脚本 —— 跨机器移植指南

> 适用范围：`dsh-desktop\` 文件夹内全部脚本（启动器 / 关闭服务 / 快捷方式创建 / 图标切换器）
> 整理日期：2026-08-22 ｜ 更新：补丁 A/B 已内置进 `switch-dsh-icon.ps1`，本文档与当前脚本版本一致
> **结论：整包拷贝即可用。** 脚本层面没有一处写死本机专属路径。

---

## 一、已经做到「路径无关」的设计 ✅

| 资源 | 解析方式 | 换机器后 |
|---|---|---|
| 脚本自身位置 | bat 用 `%~dp0`，ps1 用 `$PSScriptRoot`，全部相对引用 | ✅ 文件夹放任意位置都能跑 |
| 日志 / Chrome 配置目录 | `%LOCALAPPDATA%\DSHDesktop\` | ✅ 自动跟随新机器当前用户 |
| 桌面路径 | `[Environment]::GetFolderPath("Desktop")` | ✅ OneDrive 重定向也能正确解析 |
| dsh 命令定位 | `Get-Command dsh` → 垫片同目录推导 node.exe + bin.js → 兜底 `~/.nvmd/bin/dsh.cmd` | ✅ npm / nvmd 安装均可 |
| 浏览器（启动器） | ProgramFiles → ProgramFiles(x86) → 用户目录 多候选探测，Chrome→Edge 兜底，支持 `-ChromePath` 参数覆盖 | ✅ |
| 服务端口 | 3080 → 3081 → 3082 依次探测，以页面包含 `__DSH_BOOT__` 标记识别 DSH | ✅ |
| 图标库 / 状态 | `icons\` 相对目录；`active-icon.txt` 只记录图标**名称**不记录路径 | ✅ 库随文件夹走，状态仍有效 |
| 编码 | ps1 全部带 UTF-8 BOM；bat 内容纯 ASCII | ✅ 任何系统区域设置不乱码 |
| 权限 | 所有入口都带 `-ExecutionPolicy Bypass` | ✅ 不依赖目标机器执行策略配置 |

---

## 二、原两处环境依赖 —— 已内置解决 ✅

### 依赖 1：渲染 SVG 的浏览器 ✅ 已内置多候选探测

- **现状（已升级）**：脚本内 `Resolve-HeadlessBrowser` 按
  `ProgramFiles → ProgramFiles(x86) → 用户目录 → Edge → PATH` 顺序自动探测，无需任何配置。
- 仅当机器上 **Chrome 和 Edge 都没装**时，应用 SVG 桌面图标会提示找不到浏览器；
  届时安装任意其一即可，或临时改用 `.png` / `.ico` 格式图标。

### 依赖 2：任务栏图标定位 DSH 前端 dist ✅ 已内置环境变量逃生口

- **现状（已升级）**：`Get-DistDir` 探测链 =
  **`DSH_FRONTEND_DIST` 环境变量（最高优先）** → `dsh` 垫片同目录 → nvmd versions 全版本扫描 → 全局 npm。
- 特殊安装布局的机器上手动指定一次即可：

```powershell
setx DSH_FRONTEND_DIST "D:\你的路径\dsh-web-frontend\dist"
```

---

## 三、前提条件（目标机器必须满足）

1. **已安装 DeepSeek Harness 本体**（`dsh` 命令可用）——这套脚本是 DSH 的「遥控器」，不含 DSH 本身；
2. Windows 10 / 11（用到 WinForms 居中、CIM 进程查询、ie4uinit、Chrome/Edge 无头截图）；
3. 无需联网，全部本地操作；
4. 无需管理员权限（所有写入都在当前用户目录内）。

---

## 四、移植三步操作

```text
1. 把整个 dsh-desktop\ 文件夹原样拷贝到新电脑任意位置
   （路径随意——所有引用都是相对的）

2. 双击 DSH桌面版.bat
   → 自动探测端口；服务没起就后台拉起 dsh web，然后打开独立窗口

3. 双击 创建桌面快捷方式.bat
   → 在新机器桌面生成带图标的快捷方式
```

图标还原：`active-icon.txt` 记录的是图标名、`icons\` 库随文件夹走，
到新机器后执行一次 `switch-dsh-icon.ps1 -Reapply` 即可还原你选好的桌面/任务栏组合。

---

## 五、传输小坑 🕳️

通过压缩包 / 网络下载 / 共享盘拷贝后，Windows 会给文件打上「来自其他计算机」的锁定标记（Mark of the Web），首次运行可能被拦截。

**处理**（任选其一）：
- 右键每个被拦的文件 → 属性 → 勾选「解除锁定」→ 确定；
- 或在 PowerShell 里对文件夹整体解锁：
  ```powershell
  Get-ChildItem "D:\你的路径\dsh-desktop" -Recurse | Unblock-File
  ```

---

## 六、故障速查

| 现象 | 原因 | 处理 |
|---|---|---|
| 启动报「找不到 dsh 命令」 | 目标机器未装 DSH 或不在 PATH | 安装 DeepSeek Harness；或确认 `dsh --version` 可用 |
| 提示「无法启动 DSH Web 服务」 | 三个候选端口都被占 / dsh web 启动失败 | 查日志 `%LOCALAPPDATA%\DSHDesktop\start.log` 与 `server-*.err.log` |
| 切 SVG 图标报「找不到可用的 Chrome / Edge」 | 机器上没有 Chromium 系浏览器 | 安装 Chrome 或 Edge；或临时改用 .png/.ico 图标 |
| 切任务栏图标报「未定位到 dist」 | 特殊安装布局，自动探测未命中 | 设置环境变量 `DSH_FRONTEND_DIST` 指向 dist 目录 |
| dsh 升级后任务栏图标变回原样 | dist 被升级重置（正常现象） | 执行 `switch-dsh-icon.ps1 -Reapply` |
| 桌面图标显示旧样子 | Windows 图标缓存 | F5 刷新桌面；切换器已自动广播缓存刷新，一般无需处理 |

---

## 七、一句话总结

**全部功能均已跨机器可移植：路径全动态解析，浏览器多候选自动探测，特殊安装布局可通过 `DSH_FRONTEND_DIST` 环境变量兜底。**
