把想用的图标文件丢进这个目录即可被切换器识别:
  - 支持 .svg / .png / .ico (推荐 svg 或 >=256px 的 png)
  - 文件名就是菜单里的名称, 例如 whale.png 显示为 whale
  - svg 支持任意复杂度: 渐变/滤镜/圆角底板/多元素都可以,
    根标签建议带 viewBox; 缺少 width/height 时切换器会按 256px 渲染
  - .ico 直接作为快捷方式图标使用, 任务栏图标取其最大帧
